# docs/

`demo.gif` goes here — see the recording instructions in the HTML comment at the
top of the root README.

It is the one deliverable that has to be produced from a live claude.ai account
with the connector installed, so it cannot be generated from the repo.
